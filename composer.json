{
    "name": "zergius-eggstream/amp-converter",
    "description": "Convert rendered HTML pages into AMP-valid HTML. Framework-agnostic with an optional Symfony bridge.",
    "type": "library",
    "license": "MIT",
    "keywords": ["amp", "html", "converter", "symfony", "static-site"],
    "require": {
        "php": ">=8.4",
        "ext-libxml": "*",
        "ext-mbstring": "*",
        "ext-simplexml": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0",
        "phpstan/phpstan": "^2.0"
    },
    "suggest": {
        "symfony/http-kernel": "Required to use the bundled Symfony bridge (auto-wiring, config tree, bundle)",
        "symfony/dependency-injection": "Required to use the bundled Symfony bridge"
    },
    "autoload": {
        "psr-4": {
            "ZergiusEggstream\\AmpConverter\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "ZergiusEggstream\\AmpConverter\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "phpstan": "phpstan analyse"
    },
    "config": {
        "sort-packages": true
    }
}
